/** The four screens of the desk. Its own module so the navigation and the app
 *  can share the type without either importing the other. */
export type View = 'console' | 'setup' | 'functions' | 'plan'
